- name: SYSTEMS
  slug: systems
  width: default
  columns:
    - size: small
      widgets:
        - $include: ../widgets/today-health.yml
        - $include: ../widgets/safe-links.yml
    - size: full
      widgets:
        - $include: ../widgets/instance-metrics.yml
        - $include: ../widgets/github-status.yml
